{
  "name": "supermercado-chile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "check": "npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
